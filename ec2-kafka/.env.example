# Copia a ".env" y completa.
# IP o DNS de ESTA maquina (ec2-kafka), visible desde ec2-apps.
# Se usa como "advertised listener": los micros se conectan a estas direcciones.
KAFKA_EXTERNAL_HOST=10.0.0.20
